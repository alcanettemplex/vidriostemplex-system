node_modules/
dist/
build/
.env
.env.*
*.log
.DS_Store

.claude/

documentation/*.XLS

# Scripts one-off ya ejecutados (no versionados)
backend-api/src/scripts/fix_fecha_corte_importacion_2026-07-06.js
backend-api/src/scripts/importar_buscador_leads_2026-07-06.js

# WhatsApp Web Session Cache
.wwebjs_auth/
backend-api/.wwebjs_auth/

# Canvas de diseño construido (~2 MB, editor embebido). Se regenera desde las
# fuentes .dc.html + canvas.json de design/ — ver design/proveedores/README.md
design/**/*.html
!design/**/*.dc.html

# Notas de traspaso entre sesiones de trabajo. Se quedan en local a propósito:
# documentan cómo probar contra el sistema real —incluido firmar un JWT con el
# JWT_SECRET del .env para entrar como root sin contraseña, porque
# authMiddleware valida la firma y no consulta la BD— y eso es una receta de
# escalada de privilegios que no tiene por qué viajar por git. Lo que sí debe
# quedar versionado es la bitácora: va en SESSION_LOG.md, sin esos detalles.
HANDOFF.md
